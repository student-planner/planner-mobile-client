import 'package:flutter/material.dart';

/// vertical: 24, horizontal: 22
const kThemeDefaultPadding = EdgeInsets.symmetric(
  vertical: 24,
  horizontal: 22,
);

/// horizontal: 22
const kThemeDefaultPaddingHorizontal = EdgeInsets.symmetric(
  horizontal: 22,
);

/// vertical: 30, horizontal: 22
const kBottomSheepDefaultPaddingHorizontal = EdgeInsets.symmetric(
  vertical: 30,
  horizontal: 22,
);

/// all: 20
const kBottomSheepDefaultPadding = EdgeInsets.all(20);

/// vertical: 20, horizontal: 16
const kCardBottomSheepDefaultPadding = EdgeInsets.symmetric(
  vertical: 20,
  horizontal: 16,
);

/// BorderRadius.circular(16)
final kBorderRadius = BorderRadius.circular(16);
